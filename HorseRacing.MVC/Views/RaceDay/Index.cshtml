@model IEnumerable<HorseRacing.Domain.RaceDay>

@using HorseRacing.MVC.Common;

@{
    ViewData["Title"] = "Index";
}

<h1>Race Days</h1>


<div class="table-responsive">

    <table class="table">
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="row">
                    <td>
                        <a class="btn btn-primary" asp-controller="RawRaces" asp-action="Index" asp-route-id="@item.RaceDayIdString">View</a>
                    </td>
                    <td class="col-3">
                        @item.Track.Name
                    </td>
                    <td class="col-2">
                        @item.RaceDate?.ToShortDateString()
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>
